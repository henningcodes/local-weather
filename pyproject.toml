[project]
name = "local-weather"
version = "0.1.0"
description = "ECMWF HRES 3-day forecast for Essen-Bredeney, deployed to GitHub Pages."
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0",
    "requests>=2.31",
    "matplotlib>=3.8",
]

[tool.uv]
package = false
